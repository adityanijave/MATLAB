% Operations Based On Vector Using Different Default Funcations

vec = 1:10;
disp("Vector having elements are: ");
disp(vec);

%finding max no from the vector
disp("the max number is " + max(vec));

%finding min no from the vector
disp("the min number is "+ min(vec));

%finding the sum of vectors elements
disp("the sum of vector "+ sum(vec));

%finding the product of vectors elements
disp("the product of vectors element is: "+ prod(vec)); 