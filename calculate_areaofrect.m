% calculating area of rectangle
% taking input from user for length and width

length = input("enter the length: ");
width = input("enter the width: ");

area_of_rect = length * width;
result = "the area of rect: " + area_of_rect;
disp(result);
